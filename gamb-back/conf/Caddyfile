https://localhost {
  tls /etc/caddy/certs/localhost-cert.pem /etc/caddy/certs/localhost-key.pem
  reverse_proxy web:8000
}

# O caddy por padrão já vai fazer o proxy reverso de conexões de websocket, 
# então não é necessário configurar nada a mais.
